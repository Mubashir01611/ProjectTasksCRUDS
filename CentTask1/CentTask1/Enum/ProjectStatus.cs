namespace CentTask1.Enum
{
    public enum ProjectStatus
    {
        Pending,      // Only here — project-level gate
        InProgress,
        OnHold,
        Completed,
        Cancelled
    }

}
